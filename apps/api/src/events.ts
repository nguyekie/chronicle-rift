import{Router}from'express';import{z}from'zod';import{db}from'./db.js';import{HttpError,requireAdmin,requireAuth}from'./http.js';const r=Router();const activeWhere=()=>({startsAt:{lte:new Date()},endsAt:{gt:new Date()}});r.get('/active',async(_q,s)=>s.json(await db.gameEvent.findMany({where:activeWhere(),include:{shopItems:true}})));r.use(requireAuth);r.get('/:id',async(q,s)=>{const event=await db.gameEvent.findFirst({where:{id:q.params.id,...activeWhere()},include:{shopItems:true}});if(!event)throw new HttpError(404,'EVENT_INACTIVE','Event is not active');const wallet=await db.userEventCurrency.upsert({where:{userId_eventId:{userId:q.auth!.sub,eventId:event.id}},update:{},create:{userId:q.auth!.sub,eventId:event.id}});s.json({event,wallet})});r.post('/:id/stages/:stage/complete',async(q,s)=>{const event=await db.gameEvent.findFirst({where:{id:q.params.id,...activeWhere()}});if(!event)throw new HttpError(410,'EVENT_ENDED','Event has ended');const stage=Number(q.params.stage);if(!Number.isInteger(stage)||stage<1||stage>12)throw new HttpError(400,'INVALID_STAGE','Invalid event stage');const token=50+stage*5;const wallet=await db.userEventCurrency.upsert({where:{userId_eventId:{userId:q.auth!.sub,eventId:event.id}},update:{balance:{increment:token}},create:{userId:q.auth!.sub,eventId:event.id,balance:token}});s.json({token,balance:wallet.balance})});r.post('/:id/shop/:itemId/buy',async(q,s)=>{const event=await db.gameEvent.findFirst({where:{id:q.params.id,...activeWhere()}});if(!event)throw new HttpError(410,'EVENT_ENDED','Event has ended');const item=await db.eventShopItem.findFirst({where:{id:q.params.itemId,eventId:event.id}});if(!item)throw new HttpError(404,'ITEM_NOT_FOUND','Item not found');const out=await db.$transaction(async t=>{const w=await t.userEventCurrency.findUnique({where:{userId_eventId:{userId:q.auth!.sub,eventId:event.id}}});if(!w||w.balance<item.cost)throw new HttpError(409,'INSUFFICIENT_EVENT_CURRENCY','Not enough event currency');await t.userEventCurrency.update({where:{userId_eventId:{userId:q.auth!.sub,eventId:event.id}},data:{balance:{decrement:item.cost}}});return{item,serial:item.metadata?`${event.code}-${Date.now().toString(36).toUpperCase()}`:null,balance:w.balance-item.cost}});s.json(out)});r.post('/admin/schedule',requireAdmin,async(q,s)=>{const v=z.object({code:z.string(),name:z.string(),description:z.string(),startsAt:z.coerce.date(),endsAt:z.coerce.date()}).refine(x=>x.endsAt>x.startsAt).parse(q.body);s.status(201).json(await db.gameEvent.create({data:{...v,currencyCode:'TOKEN',stages:Array.from({length:12},(_,i)=>({number:i+1}))}}))});export default r;
