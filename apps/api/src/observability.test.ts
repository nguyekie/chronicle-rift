import{describe,it,expect}from'vitest';import express from'express';import request from'supertest';import{rateLimit,metrics,telemetry}from'./observability.js';describe('production middleware',()=>{it('adds request id',async()=>{const app=express().use(telemetry).get('/',(_q,s)=>s.send('ok'));expect((await request(app).get('/')).headers['x-request-id']).toBeTruthy()});it('limits abusive clients',async()=>{const app=express().use(rateLimit(1,60000)).get('/',(_q,s)=>s.send('ok'));expect((await request(app).get('/')).status).toBe(200);expect((await request(app).get('/')).status).toBe(429)});it('exports prometheus metrics',()=>expect(metrics()).toContain('chronicle_http_requests_total'))});
